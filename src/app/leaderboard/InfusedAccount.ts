export interface InfusedAccount {
  nftMint: string;
  owner: string;
  carbonScore: number;
}

export interface LeaderBoardItem {
  nftMint: string;
  imageUri: string;
  name: string;
  collection: string;
  owner: string;
  carbonScore: number;
}
